/* eslint-disable no-undef */
require("chai").should();
const Editor = require("tiptap").Editor;

describe("Setup Tests", () => {
  it("create editor", () => {
    const editor = new Editor();

  });
});
